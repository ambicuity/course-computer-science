# Outputs

- `sample-render.txt`: Example ASCII render output from the toy browser pipeline.
- Current support: tag selectors and `.class` selectors, `color` declaration.
- Known limitations: no real HTML tokenizer, no nested layout geometry, no JS.

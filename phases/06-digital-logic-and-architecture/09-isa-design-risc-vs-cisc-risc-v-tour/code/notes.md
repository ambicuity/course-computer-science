# RISC-V ISA Notes

- Fixed-width base encoding (32-bit) simplifies decode.
- Register-register arithmetic keeps datapaths regular.
- Load/store separation keeps memory side effects explicit.
- Extensions (M, A, F, D, C, V) scale feature set modularly.
